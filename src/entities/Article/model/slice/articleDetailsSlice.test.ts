/**
 *    articleDetailsSlice test
 *      - test's for articleSlice
 *
 *    @test 'add_test_describe'.
 *
 */

import { ArticleDetailSchema } from '../types/ArticleDetailSchema';
import { articleDetailsActions, articleDetailsReducer } from './articleDetailsSlice';

const InitialState: DeepPartial<ArticleDetailSchema> = {};

// const data = {
//
// };

describe('articleDetailsSlice', () => {
  test('articleDetailsSlice reducer test', () => {
  });

  test('articleDetailsSlice reducer test', () => {
  });
});
