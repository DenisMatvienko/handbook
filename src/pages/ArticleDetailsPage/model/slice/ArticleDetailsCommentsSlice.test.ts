/**
 *    ArticleDetailsCommentsSlice test
 *      - test's for ArticleDetailsCommentsSlice
 *
 *    @test 'add_test_describe'.
 *
 */

import { add_slice_action, add_slice_reduser } from './ArticleDetailsCommentsSlice';
import { add_type_schema } from '';

const InitialState: DeepPartial<add_type_schema> = {};

const data = {
  also_can_add_manual_data,
};

describe('ArticleDetailsCommentsSlice', () => {
  test('ArticleDetailsCommentsSlice reducer test', () => {
  });

  test('ArticleDetailsCommentsSlice reducer test', () => {
  });
});
