/**
 *    articlePageSlice test
 *      - test's for articlePageSlice
 *
 *    @test 'add_test_describe'.
 *
 */

import { add_slice_action, add_slice_reduser } from './articlePageSlice';
import { add_type_schema } from '';

const InitialState: DeepPartial<add_type_schema> = {};

const data = {
  also_can_add_manual_data,
};

describe('articlePageSlice', () => {
  test('articlePageSlice reducer test', () => {
  });

  test('articlePageSlice reducer test', () => {
  });
});
