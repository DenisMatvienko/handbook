/**
 *      Search types
 *
 *      @param articleList;
 *          - Data for search request;
 */

export interface NavbarSearchSchema {
    isLoading?: boolean,
    error?: string,
    search: string,
}
