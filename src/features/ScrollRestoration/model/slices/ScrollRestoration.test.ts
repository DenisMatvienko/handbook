/**
 *    ScrollRestoration test
 *      - test's for ScrollRestoration
 *
 *    @test 'add_test_describe'.
 *
 */

import { add_slice_action, add_slice_reduser } from './ScrollRestoration';
import { add_type_schema } from '';

const InitialState: DeepPartial<add_type_schema> = {};

const data = {
  also_can_add_manual_data,
};

describe('ScrollRestoration', () => {
  test('ScrollRestoration reducer test', () => {
  });

  test('ScrollRestoration reducer test', () => {
  });
});
